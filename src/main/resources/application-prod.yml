server:
  port: 8080

spring:
  profiles:
    active: prod

jwt:
  secret: ${JWT_SECRET}

datasource:
  url: ${MYSQL_URL}
  username: ${MYSQLUSER}
  password: ${MYSQLPASSWORD}
  driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true